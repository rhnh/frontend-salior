{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "yarn debug",
      "name": "Run yarn debug",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
